
export enum NotifyType {
    SUCCESS = 'success',
    ERROR = 'error',
    WARNING = 'warning',
    INFO = 'info',
    LOADING = 'loading'
}

export enum MatSnackBarHorizontalPosition {
    CENTER = 'center',
    START = 'start',
    END = 'end',
    RIGHT = 'right',
    LEFT = 'left'
}

export enum MatSnackBarVerticalPosition {
    TOP = 'top',
    BOTTOM = 'bottom',
}

export enum ConfirmActionType {
    DELETE = 'Are you sure you want to do this?'
}

export enum ConfirmMessage {
    DELETE = "Are you sure you want to permanently delete this item? This action can't undo"
}
